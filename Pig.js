class Pig extends BaseClass {
    constructor(x, y) {

    super(x,y,50,50);      
    this.image = loadImage("sprites/enemy.png")
    }
    
  };

  //inheritance  - create sub/child classes which have some items same as the base class/parent class
  //- create once and use it multiple
  